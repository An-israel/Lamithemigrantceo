import { ComingSoon } from "@/components/admin/ComingSoon";

export default function AdminAnalyticsPage() {
  return (
    <ComingSoon title="Analytics" release="Release 3">
      Cookieless traffic analytics: visitors, top pages, traffic sources (with
      TikTok highlighted), device split and a conversion funnel. The
      `page_views` table and its privacy-preserving logging are already created.
    </ComingSoon>
  );
}
