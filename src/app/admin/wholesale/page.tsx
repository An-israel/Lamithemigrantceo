import { ComingSoon } from "@/components/admin/ComingSoon";

export default function AdminWholesalePage() {
  return (
    <ComingSoon title="Wholesale" release="Release 2">
      Add, edit, duplicate and archive wholesale bundles, with stock counts,
      multi-image upload and low-stock warnings. The `wholesale_products` and
      `restock_alerts` tables are already created.
    </ComingSoon>
  );
}
