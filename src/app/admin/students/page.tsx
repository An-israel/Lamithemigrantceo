import { ComingSoon } from "@/components/admin/ComingSoon";

export default function AdminStudentsPage() {
  return (
    <ComingSoon title="Students" release="Release 2">
      See who has enrolled, which programs they hold, and their order history.
      Built on the `users` and `orders` tables that already exist.
    </ComingSoon>
  );
}
